#!/usr/bin/expect -f

trap exit { SIGCHLD }

# Default timeout value 10
set timeout 2

log_user 0
log_file -a /root/full-upgrade.log

spawn $argv

for {} 1 {} {
	expect -re ".*\\?" { send "Y\r\n" }
}
